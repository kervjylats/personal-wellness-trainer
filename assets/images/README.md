Reserved for image assets (e.g. a custom logo or splash image) that a
buyer configuring this template might want to add. Referenced via
`AssetPaths` in `lib/core/constants/asset_paths.dart` — add new image
constants there rather than writing paths inline.

This placeholder file exists only so the folder isn't empty (git doesn't
track empty directories) — delete it once you add real images here.
